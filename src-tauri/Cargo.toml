[package]
name = "app"
version = "0.1.0"
description = "Documinter native shell"
authors = ["Florian Douay"]
license = "Apache-2.0"
repository = ""
edition = "2021"
rust-version = "1.77.2"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
name = "app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.6.3", features = [] }

[dependencies]
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
log = "0.4"
tauri = { version = "2.11.3", features = [] }
tauri-plugin-opener = "2"
tauri-plugin-fs = { version = "2", features = ["watch"] }
tauri-plugin-dialog = "2"
tauri-plugin-sql = { version = "2", features = ["sqlite"] }
tauri-plugin-persisted-scope = "2"
tauri-plugin-single-instance = "2"

# WebView2 direct-to-file PDF. Versions match what Tauri already resolves (webview2-com 0.38.2, windows
# 0.61.3), so no duplicate copy enters the tree. Windows-only, the sole target where PrintToPdf exists.
[target.'cfg(windows)'.dependencies]
webview2-com = "0.38"
windows = "0.61"

[dev-dependencies]
# FTS5 probe: open sqlx against the same SQLite layer the sql plugin wraps.
sqlx = { version = "0.8", features = ["sqlite", "runtime-tokio"] }
tokio = { version = "1", features = ["macros", "rt"] }
